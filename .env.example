# Root env — used by docker-compose only.
# App-specific env files live in apps/api/.env, apps/web/.env, apps/mobile/.env
# (each has its own .env.example).

POSTGRES_USER=divzy
POSTGRES_PASSWORD=divzy_dev_password
POSTGRES_DB=divzy
POSTGRES_PORT=5433
